pipeline {
    agent any

    stages {
        stage('Plan') {
            steps {
                sh 'terraform init -input=false'
                sh 'terraform show -no-color ../initial-hello.tf > tfplan.txt;cat tfplan.txt'
            }
        }
        stage('Apply') {
            steps {
                sh "terraform --version"
            }
        }
    }

}
